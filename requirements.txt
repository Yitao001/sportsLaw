# ============================================================
# SportsLax — 体育法律咨询智能体 依赖清单
# ============================================================

# ── Web 框架 ─────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# ── 工具 ─────────────────────────────────────────────────────
python-dotenv>=1.0.0
PyYAML>=6.0.1
watchdog>=3.0.0

# ── LangChain 核心 ───────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.2.0

# ── LLM 提供商（按需选择其中一种，但全部声明以支持切换）───
# 通义千问（阿里云 DashScope）
dashscope>=1.19.0

# Anthropic Claude
langchain-anthropic>=0.1.0

# ── 向量数据库（RAG）────────────────────────────────────────
chromadb>=0.5.0

# ── 安全 / 限流 ──────────────────────────────────────────────
slowapi>=0.1.9
limits>=3.6.0
